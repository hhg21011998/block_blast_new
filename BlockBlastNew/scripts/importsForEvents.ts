/**
 * Purpose: Imports for events.
 * Everything imported here is visible to TypeScript snippets in event sheets.
 * Event sheets cannot use import/export (they run inside a function).
 */
import * as Game from "./main.js";

export { Game };
